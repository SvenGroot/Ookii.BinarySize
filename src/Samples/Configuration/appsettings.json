{
  /* Size uses the BinarySize type, so it's interpreted as 128 * 1024 * 1024 bytes. */
  "Size": "128MB",
  /* IecSize uses the IecBinarySize type, so it's interpreted as 128,000,000 bytes. */
  "IecSize": "128MB"
}
